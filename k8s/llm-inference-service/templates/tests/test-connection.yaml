apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "llm-inference-service.fullname" . }}-test-connection"
  labels:
    {{- include "llm-inference-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "llm-inference-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
